server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/path/to/your/config-repo   # local path for now, real Git URL later
          default-label: main